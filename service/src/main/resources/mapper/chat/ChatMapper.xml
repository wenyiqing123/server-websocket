<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.wyq.serverwebsocket.mapper.ChatMapper">
    <select id="selectByUserId" resultType="cn.wyq.serverwebsocket.pojo.entity.Chat">
        SELECT id, user_id, title, created_at
        FROM ai_chat_session
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <insert id="insertSession">
        INSERT INTO ai_chat_session (id, user_id, title, created_at)
        VALUES (#{id}, #{userId}, #{title}, NOW())
    </insert>

    <delete id="deleteById">
        DELETE FROM ai_chat_session
        WHERE id = #{id}
    </delete>


</mapper>